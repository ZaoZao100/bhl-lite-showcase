# 3S 人形物联网服务系统网页交接说明

## 当前版本

- 版本提交：`3a54908`
- 公网页面：<https://zaozao100.github.io/bhl-lite-showcase/?v=3a54908>
- 技术栈：React、TypeScript、Vite、Three.js、URDF Loader
- 建议环境：Node.js 22，npm 10 或更高版本

## 快速启动

在项目根目录执行：

```bash
npm install
npm run dev
```

浏览器访问终端显示的地址。本地通常为 `http://127.0.0.1:5173/`，同一局域网中的其他设备可使用电脑局域网 IP 加端口 `5173` 访问。

## 构建成品

```bash
npm run build
npm run preview
```

- `dist/` 是已经生成的静态网页成品。
- 部署到普通服务器时，上传 `dist/` 内全部内容。
- 部署到子目录时，通过 `VITE_BASE_PATH` 指定网站根路径。

## GitHub Pages 发布

仓库已包含 `.github/workflows/deploy-pages.yml`。推送到 `main` 分支后会自动安装依赖、构建并发布。

首次换到新仓库时，在仓库的 `Settings → Pages` 中将发布来源设置为 `GitHub Actions`。

## 主要目录

- `src/`：页面、3D 场景、动作、爆炸动画与全部视觉样式。
- `public/humanoid/`：当前网页使用的 URDF、原始 STL 与 gzip 压缩网格。
- `public/robot/`：保留的另一套机器人模型资料。
- `public/photos/`：实物制造、装配与整机照片。
- `public/renders/`：整机、关节、仿真和遥操作渲染图。
- `research/`：模型资产、仿真训练、ONNX 策略、低层控制和遥操作参考资料。
- `scripts/`：桌面端、移动端与 3D 交互自动检查脚本。
- `dist/`：当前版本的静态构建成品。
- `.github/workflows/`：自动发布配置。

## 关键入口

- `src/App.tsx`：机器人加载、材质、灯光、动作、下蹲脚底锁定、爆炸动画和页面全部内容。
- `src/styles.css`：完整响应式布局、物联网系统图和视觉特效。
- `vite.config.ts`：本地服务与部署路径配置。
- `package.json`：依赖与启动命令。

## 资源注意事项

- 不要随意修改 `public/humanoid/` 内 URDF 和网格的相对路径，否则 3D 模型会加载失败。
- 页面优先读取 `meshes-gzip/` 中的压缩网格；原始 `meshes/` 作为本地和交接备份保留。
- `node_modules/` 没有收入压缩包，接手人执行 `npm install` 或 `npm ci` 即可恢复依赖。
- `.git/`、本地浏览器缓存和测试截图没有收入压缩包；源码、资源、配置、构建成品与研究资料均已保留。

## 交接前验证结果

- TypeScript 与 Vite 构建通过。
- 桌面端和 390 px 手机端无横向溢出。
- URDF 模型、全部动作、爆炸/重组、旋转交互正常。
- 下蹲动作保持待机脚底最低点不动。
- 公网 GitHub Pages 页面加载正常。
